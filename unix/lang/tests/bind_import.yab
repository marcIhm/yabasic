#!./yabasic

import library1
import library2
import library3
import library4

if (peek("isbound")) then
  exit 0
else
  bind "bind_import"
  system("chmod 755 bind_import")
  exit system("./bind_import")/256
endif  
